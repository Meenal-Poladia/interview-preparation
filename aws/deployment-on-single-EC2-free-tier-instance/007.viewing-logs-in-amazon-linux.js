/*
    Views the latest 50 logs
    sudo journalctl -n 50 -u pomodoro-timer.service

    Views all logs
    journalctl -u pomodoro-timer.service

    To view logs as they are generated
    sudo journalctl -f -u pomodoro-timer.service


 */